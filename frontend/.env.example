# Values prefixed with VITE_ are embedded in the browser bundle and are public.
# Never place a Supabase service-role key or another private credential here.
VITE_API_URL=http://localhost:8000
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
